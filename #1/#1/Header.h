#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <math.h>

#pragma warning(disable:4996)

int Enter(int**, int, int,int);
void Remover(int**, int, int, int,int);
void Printer(int**, int, int);
